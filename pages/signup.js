import {useState} from "react"; import Link from "next/link"; import {createClient} from "@supabase/supabase-js";
const supabase=process.env.NEXT_PUBLIC_SUPABASE_URL&&process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY?createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY):null;
export default function Signup(){const[email,setEmail]=useState("");const[pw,setPw]=useState("");const[msg,setMsg]=useState("");async function submit(e){e.preventDefault();if(!supabase)return setMsg("Supabase environment variables are not configured.");const{error}=await supabase.auth.signUp({email,password:pw,options:{emailRedirectTo:location.origin+"/callback"}});setMsg(error?error.message:"Check your email to confirm your account.");}return <main className="auth"><div className="card"><Link href="/">← NovaSaaS</Link><h1>Create your account</h1><form onSubmit={submit}><input type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input type="password" placeholder="Password" value={pw} onChange={e=>setPw(e.target.value)} minLength="6" required/><button className="button">Create account</button></form>{msg&&<p>{msg}</p>}<small>Already have an account? <Link href="/login">Log in</Link></small></div></main>}